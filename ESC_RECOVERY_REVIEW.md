# ESC 恢复流程（2026-09-19）

## 行为

1. 找不到目标触发原有卡死/导航超时后，向所配置的模拟器发送一次 ESC。
2. 单按钮保护仍为10次、交替按钮保护仍为各6次。触发后暂停重复操作，等待普通响应预算（普通模式60秒、低端机180秒）；期间找到不同目标可继续，否则执行ESC。
3. ESC 后丢弃旧截图匹配缓存和页面位置缓存，重新截图。若识别到用户提供的“确认退出战斗吗？”标题及取消/确认按钮组，点击右侧确认；每次ESC至多点一次此确认，避免连续误点。
4. 使用注册页面的两帧识别重新定位当前页面，找到页面后回到当前任务的目标匹配流程；不会从头重跑整个任务。导航动作若在刷新画面期间发生ESC，会丢弃退出前准备好的动作。
5. 每次ESC后保留完整识别等待周期，不能连续发三次。恢复次数在同一次任务内累计，不因普通点击或重新识别成功而清零，最多发送三次ESC。
6. 第三次ESC后仍未恢复，再次超时时结束本次任务，记录失败并按该任务的失败间隔调度；不因此直接调用游戏重启。脚本原有连续任务失败停止规则保留。

普通无操作检测仍为60秒，低端机180秒；战斗等长等待仍为300秒；低端机登录每轮独立等待仍为900秒。确认帧数仍会影响实际触发时间。模拟器/游戏已经退出时仍走原有游戏未运行处理；无法发送ESC则结束本次任务，避免假装恢复。

## 输入与识别

- 窗口消息控制：向已配置模拟器根窗口发送一次ESC按下/抬起，不向桌面当前焦点发键。
- 其他控制后端：经当前设备ADB发送 `KEYCODE_ESCAPE`，不把ESC替换成任意桌面热键。
- 使用用户提供的原始弹窗截图作模板，分别校验标题和按钮组，并支持一定缩放范围。非ESC恢复阶段不自动点击该弹窗。
- 弹窗皮肤、文字或显示比例差异过大时可能不能识别，将继续受三次恢复限制；不按固定屏幕坐标盲点确认。

## 验证

离线测试覆盖三次上限/第四次终止、等待期间不重复发键、重复点击抑制、原点击和战斗等待阈值、按键后不发送旧画面动作、当前设备按键路由、任务失败不重启、真实提供截图多比例识别、缺标题/缺按钮拒绝识别、先确认后定位页面、页面缓存清理和异常清理。

未操作正在运行的模拟器或游戏，尚未完成MuMu实机按键和皮肤匹配验收。修改前文件备份到工作目录，部署前逐文件校验哈希；修改用户配置不属于本次操作。
