# 低端机适配与后台控制台修复

## 使用

在脚本设置的设备分组启用“低端机模式”（旧界面可能仍显示“低配模式”），停止并重新启动脚本。默认关闭，兼容旧配置，未改动用户配置文件。

| 项目 | 普通模式 | 低端机模式 |
| --- | --- | --- |
| 无操作卡死检测时间阈值 | 60 秒 | 180 秒 |
| 战斗等长等待时间阈值 | 300 秒 | 900 秒 |
| 登录总时限 | 原逻辑 | 900 秒，不被点击刷新 |
| 启动黑屏探测 | 15 秒 | 45 秒 |
| 通用界面等待 | 原时限 | 3 倍 |
| 普通/战斗截图最小间隔 | 原配置及后端规则 | 0.8 / 1.5 秒 |
| 两次受检操作最小间隔 | 原逻辑 | 1.2 秒 |
| 单按钮连续点击保护 | 10 次 | 30 次 |

卡死检测仍保留原来的确认帧数，因此实际触发时间也受截图速度影响。低端机登录使用独立总时限，结束或异常都会清除登录保护状态。OCR 模型与识别阈值不变。任务自身单独定义的特殊计时器不做全局倍乘。

## 黑框

ADB、Git 更新检查、安装更新命令、模拟器后台控制入口设置无控制台启动；显式要求显示模拟器的路径仍遵守原设置。

新增 `oas-silent.vbs`，双击可通过 `pythonw.exe` 无控制台启动 OAS 后端，再使用原 OASX 界面连接。不要在后端已经运行时重复启动。原 `oas.exe` 为预编译启动器，本次未重新编译；若黑框来自它自身的批处理启动阶段，请使用该静默入口。

## 自动审核与验证（2026-09-17）

- 22 项离线回归测试通过，包含原有 8 项拟人点击测试。
- 模拟慢设备超过普通等待仍继续、最终超时仍退出，验证登录标记跨点击保留、总时限不刷新、异常后清理。
- 验证单按钮/交替点击保护、实例间状态隔离、scrcpy 与 nemu_ipc 限速、截图设置不被低端机下限覆盖。
- 修复通用等待调用的参数错位，并优先检查慢截图中已出现的目标，再判断等待超时。
- 验证 ADB/Git 后台进程无控制台参数；检查其余后台启动入口。
- 实际 Pydantic 配置模型验证默认关闭、布尔设置和中文界面标题/帮助文本。
- 所有改动 Python 文件语法检查及翻译 XML 解析通过。

审核未发现阻断问题。未进行低端设备实机登录、完整任务运行和黑框复现；执行时 OAS 未运行，无法将用户观察到的黑框唯一归因于某个进程。上述测试属于离线逻辑与启动参数验证，不代表已完成实机验收。

修改前文件另行备份；部署过程逐个校验原文件哈希，防止覆盖期间出现的新改动。
