# 本地错误经验学习

在“脚本设置 → 设备 → 本地错误经验学习”开关控制，默认开启；修改后下次任务生效。无需联网或额外模型。

## 本次学习范围

针对现有 ESC 恢复流程中的界面卡住、找不到目标和重复点击，记录：任务与设备范围、错误类型、画面低分辨率特征、恢复时是否确认退出战斗、重新识别的页面、等待耗时及最终任务结果。其他异常按类型统计，供排查，不自动生成修复代码。

相同任务、配置、设备、控制方式和低端机模式下，画面尺寸一致且特征相似，并在不同运行中连续至少两次恢复后任务**显式报告成功**，经验才可复用。仅找到页面、任务延期、提前结束、错误结束，都不能当成成功经验。任务内再次恢复时，前一恢复保守记为未解决。

复用时仍按原规则发送 ESC，优先采用已验证的短等待时间（0.8～5秒），优先验证上次成功识别的页面；若该页面不匹配，立即退回完整页面识别。退出战斗确认仍必须实时识别特定标题和按钮，不能使用历史坐标盲点。经验仅改变等待和识别优先顺序，不引入新动作、不跳过超时、不修改任务代码。

复用后失败的经验立即失去复用资格；需要之后两次独立成功才能恢复资格。经验超过30天不再复用。仍保留单按钮10次、战斗300秒、最多3次ESC等限制。

## 存储与管理

数据库首次有需记录的事件时才创建：`config/error_learning.sqlite3`。不存完整截图、账号密码或异常消息正文；不上传数据。保留最近最多5,000条恢复观察及1,000种异常统计，数据库加入Git忽略规则。数据库损坏、锁定或无法写入时，日志提示并继续原恢复行为。

在 OAS 根目录运行：

```powershell
.\toolkit\python.exe .\dev_tools\error_learning.py show
.\toolkit\python.exe .\dev_tools\error_learning.py export --output learning-report.json
.\toolkit\python.exe .\dev_tools\error_learning.py clear --all
```

清空前先停止脚本，避免运行中的记录重新写回；清空后重新启动脚本。导出文件包含配置名及设备标识，是否分享由用户决定。

## 验证与边界

离线回归覆盖跨运行学习、成功资格、失败停用、重新学习、配置/设备/画面隔离、过期与非法动作拒绝、数据库异常回退、识别优先及失败回退、三次ESC上限、显式任务成功判定、导出和清空。

这是保守的经验复用模块，不是“任何报错都会自我修复”。模板失效、未识别的新弹窗、程序缺陷和环境故障仍需针对性修复。尚未在MuMu实机完成多轮任务学习验收。
