# 点击实现审核（2026-09-16）

基于本地提交 `8328e623c6b10de1226e8d572369f39f2d831b41` 和用户提供的 `Random click.py`。

## 接入范围

- `module/device/human_click.py` 仅依赖 Python 标准库，包含 TargetBox、截断高斯采样、贝塞尔轨迹、最小抖动速度曲线、过冲回拉、对数正态间隔及驻留、Session。
- 普通矩形 RuleClick 和通过 RuleClick 取点的 RuleImage 保持原 ROI 半开边界。整数取整后再次检查边界。固定坐标不额外随机偏移。
- 已有 RuleScatter 是独立的多边形采样器，保留其形状约束和现有分布；未将多边形扩大为矩形。
- 所有经 Control.click 的点击接入会话节奏。直接调用底层 click_adb 等接口的调用不经过该控制层。
- window_message：先发送无按键的 WM_MOUSEMOVE 轨迹，再按原普通点击窗口路由执行 down/dwell/up；等待被中断时在 finally 中释放。
- minitouch、scrcpy、uiautomator2：使用既有定点按下/释放接口指定驻留时间。ADB 使用既有同坐标 input swipe 实现定点驻留，不发送移动轨迹。Android 触摸后端不模拟鼠标悬停。
- 轨迹中间点保留 0.6 像素抖动；首尾点精确，避免末端抖动使点击越界。两轴使用同一个过冲比例。已采样矩形通过一次性上下文传递实际短边；无矩形元数据时按精确点宽度 1 估算。
- 会话休息延续原脚本参数：18–45 次点击后休息，75% 为 1.2–4 秒，25% 为 12–90 秒。点击日志在休息前输出，休息后重置设备卡死计时。其他任务级墙钟超时仍按原逻辑计时，实际任务节奏需要模拟器验证。
- 任务启动的 reset_task_points 钩子重置会话和前次悬停位置。原任务 interval 门限不修改。
- 未修改滑动、拖拽和显式长按函数体，也未修改 assets 或配置文件。

## 复审修复

1. 修复 scrcpy 普通点击未接入、实际回退 ADB 的遗漏。
2. 将采样驻留接入所有上述控制后端。
3. 恢复 window_message 普通点击的原窗口路由，避免调用长按路径导致路由变化。
4. 删除轨迹宽度固定为 40 的假设，恢复中间轨迹抖动并保证精确终点。
5. 会话按任务重置；休息明确记录，避免设备卡死计时包含有意休息。
6. 加入空整数区域、非有限坐标和错误采样参数的检查。

## 验证

运行 `toolkit/python.exe -B -m unittest discover -s tests -p test_human_click.py -v`。

8 项离线测试通过：矩形/小数/单像素边界，非法区域，元数据消耗和重置，50 个随机种子下轨迹终点与总时长，控制后端驻留分发和无触摸移动，已有等待与会话休息，休息后的卡死计时重置，Windows 悬停消息与中断后的释放。

控制层和 Windows 类测试执行真实源码类，硬件依赖替换为模拟对象；它们不能证明模拟器实际接收事件。未执行真实游戏点击，未验证端到端游戏任务。原滑动/拖拽函数使用 AST 与基线逐项比较。

## GitHub 同步

同步目标为个人仓库 `tomoe1-1/OnmyojiAutoScript` 的 `self` 分支。已确认账号拥有写入权限，提交前远程分支与本地审核基线均为 `8328e623c6b10de1226e8d572369f39f2d831b41`。

本次提交包含四个实现文件、本审核说明及离线测试，共 6 个文件；保留原提交历史和其他仓库文件。配置、日志、游戏账号数据和本地备份不在同步范围内。
